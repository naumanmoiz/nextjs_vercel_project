import { Inter, Lusitana } from 'next/font/google'

const inter = Inter({ subsets: ['latin'], display: 'swap' });
const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'], display: 'swap' });

export { inter, lusitana }
